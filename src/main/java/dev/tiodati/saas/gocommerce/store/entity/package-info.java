/**
 * JPA entities for store management and multi-tenant data models.
 *
 * <p>
 * This package contains persistence entities that define the store structure,
 * store configuration, administrative relationships, and other core data models
 * for the multi-tenant store system. These entities support both master schema
 * (platform-level) and store-specific schema operations.
 * </p>
 *
 * @since 1.0
 */
package dev.tiodati.saas.gocommerce.store.entity;
