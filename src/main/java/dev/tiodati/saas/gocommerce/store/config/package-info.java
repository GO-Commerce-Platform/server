/**
 * Configuration classes for multi-tenant store management and schema
 * resolution.
 *
 * <p>
 * This package contains configuration beans, resolvers, and setup classes that
 * enable the multi-tenant architecture. Key components include tenant
 * resolvers, schema configuration, and store context initialization that
 * support proper isolation and routing of data operations across different
 * store schemas.
 * </p>
 *
 * @since 1.0
 */
package dev.tiodati.saas.gocommerce.store.config;
